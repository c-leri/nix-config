;; Include widgets
(include "eww_widgets.yuck")

;; Windows

;; Powermenu
(defwindow powermenu
  :monitor 0
  :stacking "overlay"
  :geometry (geometry :width "100%" :height "100%")
  (powermenu_layout))

;; Fake tray
(defwindow faketray
  :monitor 0
  (systray))
